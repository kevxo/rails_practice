<div>
  <h1>Children</h1>

  <% @kids.each do |child| %>
  -------------------------------------
  <ul class="child-<%= child.id %>">
      <li>Name: <%= child.name %> - <%= link_to 'Edit', "/childrens/#{child.id}/edit" %></li>
      <li>Age: <%= child.age %></li>
  </ul>
  <% end %>
</div>